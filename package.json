{ "name": "site",
  "version": "1.0.0",
  "description": "A personal website",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export -o docs && touch docs/.nojekyll && touch docs/CNAME && echo \"vitalivoi.com\" >> docs/CNAME"
  },
  "dependencies": {
    "next": "^9.1.1",
    "react": "^16.11.0",
    "react-dom": "^16.11.0"
  }
}